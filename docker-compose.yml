services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: face_recognition
      POSTGRES_USER: face_app
      POSTGRES_PASSWORD: face_app_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pgdata:
